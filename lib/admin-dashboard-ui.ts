/** `id` on the site-content `<form>` in AdminDashboard; external submit buttons use `form={...}`. */
export const ADMIN_SITE_CONTENT_FORM_ID = "admin-dashboard-site-content-form";

/** Empty host in `app/(admin)/admin/layout.tsx` (mobile save button is portaled here). */
export const ADMIN_MOBILE_SAVE_PORTAL_ID = "admin-mobile-save-portal";
